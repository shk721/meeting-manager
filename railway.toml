[build]
builder = "nixpacks"
buildCommand = "pnpm --filter @workspace/db run push-force && pnpm --filter @workspace/meeting-manager run build && pnpm --filter @workspace/api-server run build"

[deploy]
startCommand = "NODE_ENV=production node --enable-source-maps ./artifacts/api-server/dist/index.mjs"
