[build]
builder = "nixpacks"
buildCommand = "pnpm --filter @workspace/meeting-manager run build && BASE_PATH=/dt/ pnpm --filter @workspace/dt-dashboard run build && pnpm --filter @workspace/api-server run build"

[deploy]
startCommand = "pnpm --filter @workspace/db run push-force && NODE_ENV=production node --enable-source-maps ./artifacts/api-server/dist/index.mjs"
